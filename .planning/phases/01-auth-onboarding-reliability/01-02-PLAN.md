---
phase: 01-auth-onboarding-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-new/app/onboarding/page.tsx
  - frontend-new/app/api/preferences/route.ts
  - frontend-new/app/meals/page.tsx
autonomous: true
requirements:
  - ONB-01
  - ONB-02
  - ONB-03
user_setup: []
must_haves:
  truths:
    - "Onboarding completion is blocked only when canonical `/api/preferences` save fails."
    - "Secondary profile/allergy sync failures are non-blocking and surfaced with warning + retry."
    - "Onboarding defaults path still produces a usable starter flow."
  artifacts:
    - path: "frontend-new/app/api/preferences/route.ts"
      provides: "single-writer preference/profile persistence with compatibility fallback"
      exports: ["POST"]
    - path: "frontend-new/app/onboarding/page.tsx"
      provides: "onboarding submit flow using canonical API response semantics"
      min_lines: 400
    - path: "frontend-new/app/meals/page.tsx"
      provides: "preference save panel consuming partial success fields safely"
      min_lines: 400
  key_links:
    - from: "frontend-new/app/onboarding/page.tsx"
      to: "/api/preferences"
      via: "single POST persistence call"
      pattern: "fetch\\(\"/api/preferences\""
    - from: "frontend-new/app/api/preferences/route.ts"
      to: "profiles and user_allergies tables"
      via: "best-effort secondary sync after canonical user_preferences write"
      pattern: "profileSaved|allergiesSynced"
---

<objective>
Stabilize onboarding persistence by enforcing `/api/preferences` as the canonical write path.

Purpose: satisfy `ONB-01`, `ONB-02`, and `ONB-03` with deterministic save semantics and compatible fallback writes.
Output: onboarding and preferences-save flows that block only on canonical failure and warn on secondary sync issues.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-auth-onboarding-reliability/01-CONTEXT.md
@.planning/phases/01-auth-onboarding-reliability/01-RESEARCH.md

@frontend-new/app/onboarding/page.tsx
@frontend-new/app/api/preferences/route.ts
@frontend-new/app/meals/page.tsx
@frontend-new/lib/onboarding-meta.ts
</context>

<threat_model>
## Trust Boundaries
- Onboarding form payload -> `/api/preferences`
- Preferences API -> Supabase `user_preferences`, `profiles`, `user_allergies`
- Meals preferences panel -> same persistence API contract

## STRIDE Register
- threat_id: T-01-04
  category: Tampering
  component: preference payload normalization
  disposition: mitigate
  mitigation_plan: "Normalize and bound goal/dietary/macro fields server-side before any upsert attempts."
- threat_id: T-01-05
  category: Repudiation
  component: partial-save behavior
  disposition: mitigate
  mitigation_plan: "Return explicit `profileSaved` and `allergiesSynced` fields for each save, and log structured route-context on fallback failures."
- threat_id: T-01-06
  category: Denial of Service
  component: repeated onboarding retries
  disposition: mitigate
  mitigation_plan: "Keep canonical save idempotent (`upsert` on `user_id`) and avoid extra direct-table writes from frontend."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate direct persistence from onboarding page</name>
  <files>frontend-new/app/onboarding/page.tsx</files>
  <read_first>frontend-new/app/onboarding/page.tsx, frontend-new/app/api/preferences/route.ts, .planning/phases/01-auth-onboarding-reliability/01-CONTEXT.md</read_first>
  <action>Refactor onboarding submit flow so `/api/preferences` is the single authoritative write path. Remove direct `profiles` and `user_allergies` writes from onboarding submit handler when equivalent server handling already exists. Keep local onboarding meta persistence and route transitions (`/meals?onboarding=ready` and `/meals?onboarding=skipped`) unchanged.</action>
  <verify>rg -n "fetch\\(\"/api/preferences\"|profiles\\.upsert|saveAllergiesToTable" frontend-new/app/onboarding/page.tsx</verify>
  <acceptance_criteria>
    - `frontend-new/app/onboarding/page.tsx` still posts to `/api/preferences`
    - `frontend-new/app/onboarding/page.tsx` no longer contains onboarding submit path direct writes to `profiles` table
    - `frontend-new/app/onboarding/page.tsx` no longer calls `saveAllergiesToTable` from submit handler
  </acceptance_criteria>
  <done>Onboarding page delegates persistence responsibility to the canonical API endpoint only.</done>
</task>

<task type="auto">
  <name>Task 2: Harden `/api/preferences` compatibility fallback and response semantics</name>
  <files>frontend-new/app/api/preferences/route.ts</files>
  <read_first>frontend-new/app/api/preferences/route.ts, frontend-new/lib/serverAuth.ts, .planning/phases/01-auth-onboarding-reliability/01-CONTEXT.md</read_first>
  <action>Keep canonical `user_preferences` upsert as the only blocking operation and preserve fallback payload variants for known schema differences (`goal`, `lifestyle`, `meal_complexity`). Ensure response payload always includes `{ ok, profileSaved, allergiesSynced }` on successful canonical save. Keep structured error handling for invalid JSON and unauthorized requests with deterministic 400/401 responses.</action>
  <verify>rg -n "profileSaved|allergiesSynced|Failed to save user_preferences|status: 400|status: 401" frontend-new/app/api/preferences/route.ts</verify>
  <acceptance_criteria>
    - `frontend-new/app/api/preferences/route.ts` blocks only when canonical `user_preferences` write fails
    - `frontend-new/app/api/preferences/route.ts` returns `ok: true` plus `profileSaved` and `allergiesSynced` on success
    - `frontend-new/app/api/preferences/route.ts` preserves `400` invalid JSON and `401` unauthorized responses
  </acceptance_criteria>
  <done>Preferences API enforces single-writer ownership with deterministic primary vs secondary save behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Surface non-blocking secondary sync failures with retry affordance</name>
  <files>frontend-new/app/onboarding/page.tsx, frontend-new/app/meals/page.tsx</files>
  <read_first>frontend-new/app/onboarding/page.tsx, frontend-new/app/meals/page.tsx, frontend-new/app/api/preferences/route.ts</read_first>
  <action>Update onboarding and meals preferences-save UX to treat `{ profileSaved: false }` or `{ allergiesSynced: false }` as warning state (not hard failure). Add explicit, subtle warning copy and an actionable retry path while allowing progression when canonical save succeeded.</action>
  <verify>rg -n "profileSaved|allergiesSynced|warning|Retry" frontend-new/app/onboarding/page.tsx frontend-new/app/meals/page.tsx</verify>
  <acceptance_criteria>
    - `frontend-new/app/onboarding/page.tsx` does not block progression when canonical save is successful but secondary sync fails
    - `frontend-new/app/meals/page.tsx` logs/shows warning for partial success without throwing hard error
    - Both files expose a retry affordance for secondary sync failure paths
  </acceptance_criteria>
  <done>Users can finish onboarding reliably while still being informed about recoverable secondary sync issues.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint --prefix frontend-new`
- [ ] `rg -n "fetch\\(\"/api/preferences\"|profileSaved|allergiesSynced" frontend-new/app/onboarding/page.tsx frontend-new/app/meals/page.tsx frontend-new/app/api/preferences/route.ts`
- [ ] Manual check: onboarding default and full flows both reach `/meals` with expected notice behavior
</verification>

<success_criteria>
- All tasks completed
- `ONB-01`, `ONB-02`, and `ONB-03` are addressed by canonical save semantics
- Onboarding no longer depends on duplicate page-level direct persistence writes
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-onboarding-reliability/01-02-SUMMARY.md`
</output>
