---
phase: 01-auth-onboarding-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-new/lib/serverAuth.ts
  - frontend-new/lib/supabaseServer.ts
  - frontend-new/app/auth/callback/page.tsx
  - frontend-new/app/meals/page.tsx
  - frontend-new/app/api/meal-plan/state/route.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - LOG-02
user_setup: []
must_haves:
  truths:
    - "Protected routes only redirect to /login on explicit unauthorized outcomes."
    - "Cookie-first auth with bearer fallback remains functional for meal plan state APIs."
    - "Transient auth/session uncertainty does not create redirect loops."
  artifacts:
    - path: "frontend-new/lib/serverAuth.ts"
      provides: "authoritative auth resolution with explicit unauthorized vs fallback handling"
      min_lines: 80
    - path: "frontend-new/app/meals/page.tsx"
      provides: "bounded retry and redirect guard behavior for protected fetch flows"
      min_lines: 400
    - path: "frontend-new/app/api/meal-plan/state/route.ts"
      provides: "stable auth handling for plan state read/write with clear error semantics"
      exports: ["GET", "POST"]
  key_links:
    - from: "frontend-new/app/meals/page.tsx"
      to: "frontend-new/lib/serverAuth.ts"
      via: "shared expectation of explicit unauthorized semantics"
      pattern: "unauthorized|401|403"
    - from: "frontend-new/app/api/meal-plan/state/route.ts"
      to: "frontend-new/lib/serverAuth.ts"
      via: "resolveRequestAuth for cookie and bearer fallback"
      pattern: "resolveRequestAuth"
---

<objective>
Consolidate auth/session handling so protected page/API flows are reliable and predictable.

Purpose: satisfy `AUTH-01`, `AUTH-02`, and `LOG-02` by removing ambiguous redirect behavior.
Output: shared auth certainty behavior in server and client code paths, with bounded retries and explicit unauthorized handling.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-auth-onboarding-reliability/01-CONTEXT.md
@.planning/phases/01-auth-onboarding-reliability/01-RESEARCH.md

@frontend-new/lib/serverAuth.ts
@frontend-new/lib/supabaseServer.ts
@frontend-new/app/auth/callback/page.tsx
@frontend-new/app/meals/page.tsx
@frontend-new/app/api/meal-plan/state/route.ts
</context>

<threat_model>
## Trust Boundaries
- Browser client -> Next.js route handlers (`Authorization` header, cookies)
- Next.js route handlers -> Supabase auth/session APIs
- Protected planner page redirects -> session state interpretation

## STRIDE Register
- threat_id: T-01-01
  category: Spoofing
  component: server auth resolution
  disposition: mitigate
  mitigation_plan: "Only treat requests as unauthorized when `resolveRequestAuth` confirms no valid user or 401/403 auth response."
- threat_id: T-01-02
  category: Denial of Service
  component: client retry/redirect loop
  disposition: mitigate
  mitigation_plan: "Keep bounded retry counts and avoid redirecting during transient failures/timeouts."
- threat_id: T-01-03
  category: Information Disclosure
  component: auth-related logs
  disposition: mitigate
  mitigation_plan: "Log route-context and status only; never log raw access tokens or cookie values."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Harden server auth certainty semantics in shared auth layer</name>
  <files>frontend-new/lib/serverAuth.ts, frontend-new/lib/supabaseServer.ts</files>
  <read_first>frontend-new/lib/serverAuth.ts, frontend-new/lib/supabaseServer.ts, .planning/phases/01-auth-onboarding-reliability/01-CONTEXT.md</read_first>
  <action>Update `resolveRequestAuth` to classify results into explicit unauthorized vs fallback/transient states. Preserve cookie-first behavior, keep bearer fallback, and expose enough metadata for route handlers to avoid treating transient errors as authorization failures. Keep existing anon fallback when server cookie client init fails, and ensure no token string is logged.</action>
  <verify>npm run lint --prefix frontend-new</verify>
  <acceptance_criteria>
    - `frontend-new/lib/serverAuth.ts` contains explicit branch logic for unauthorized vs transient outcomes
    - `frontend-new/lib/serverAuth.ts` still contains bearer token extraction and `createBearerClient`
    - `frontend-new/lib/serverAuth.ts` contains no logging of raw bearer token values
  </acceptance_criteria>
  <done>Shared auth resolver returns stable, safe semantics that callers can trust.</done>
</task>

<task type="auto">
  <name>Task 2: Apply explicit unauthorized handling in meal-plan state route</name>
  <files>frontend-new/app/api/meal-plan/state/route.ts</files>
  <read_first>frontend-new/app/api/meal-plan/state/route.ts, frontend-new/lib/serverAuth.ts, .planning/phases/01-auth-onboarding-reliability/01-CONTEXT.md</read_first>
  <action>Update both GET and POST handlers in `/api/meal-plan/state` to consume shared auth certainty metadata. Return `401` only on explicit unauthorized outcomes; for other auth uncertainty, preserve bounded fallback behavior and contextual logging. Keep existing payload/date validation and avoid introducing 500-class errors for authorization-state ambiguity.</action>
  <verify>rg -n "unauthorized|resolveRequestAuth|bearer fallback" frontend-new/app/api/meal-plan/state/route.ts</verify>
  <acceptance_criteria>
    - `frontend-new/app/api/meal-plan/state/route.ts` still calls `resolveRequestAuth` in both handlers
    - `frontend-new/app/api/meal-plan/state/route.ts` returns `401` only under explicit unauthorized branch
    - `frontend-new/app/api/meal-plan/state/route.ts` retains date validation responses with `400` for invalid date input
  </acceptance_criteria>
  <done>Meal-plan state API reliably handles cookie and bearer auth without ambiguous authorization failure behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Align auth callback and meals page redirects to explicit unauthorized outcomes</name>
  <files>frontend-new/app/auth/callback/page.tsx, frontend-new/app/meals/page.tsx</files>
  <read_first>frontend-new/app/auth/callback/page.tsx, frontend-new/app/meals/page.tsx, frontend-new/lib/serverAuth.ts</read_first>
  <action>Update callback and meals page guard logic so redirects to `/login` happen only after explicit unauthorized state, not transient errors. Keep bounded retry behavior in `authFetchWithRetry`, and ensure onboarding redirect logic (`/onboarding`) is triggered only after preference load confirms missing profile state rather than auth uncertainty.</action>
  <verify>rg -n "router\\.replace\\(\"/login\"\\)|authFetchWithRetry|prefLoading" frontend-new/app/auth/callback/page.tsx frontend-new/app/meals/page.tsx</verify>
  <acceptance_criteria>
    - `frontend-new/app/auth/callback/page.tsx` keeps `/login` redirect behind explicit no-user/unauthorized branch
    - `frontend-new/app/meals/page.tsx` retains bounded retry logic (`API_MAX_RETRIES`, exponential backoff)
    - `frontend-new/app/meals/page.tsx` avoids routing to `/onboarding` until auth + preference loading states are settled
  </acceptance_criteria>
  <done>Protected page routing no longer produces spurious login/onboarding loops under transient auth conditions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint --prefix frontend-new`
- [ ] `rg -n "resolveRequestAuth|unauthorized|bearer fallback" frontend-new/app/api/meal-plan/state/route.ts frontend-new/lib/serverAuth.ts`
- [ ] Manual check: login -> callback -> meals does not loop to `/login` under normal signed-in path
</verification>

<success_criteria>
- All tasks completed
- `AUTH-01`, `AUTH-02`, and `LOG-02` behaviors are covered by code-path changes
- No new 500 responses introduced in `/api/meal-plan/state` auth flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-onboarding-reliability/01-01-SUMMARY.md`
</output>
