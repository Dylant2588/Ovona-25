---
quick_id: 260811-o94
type: execute
autonomous: true
files_modified:
  - frontend-new/app/meals/page.tsx
  - frontend-new/app/api/meal-log/route.ts
must_haves:
  truths:
    - "A signed-in user lands on a clear Today view that foregrounds the meals relevant to the current date and exposes the rest of the week without losing planner controls."
    - "A user can mark a planned meal as eaten with one lightweight action and receives calm, non-punitive confirmation."
    - "The day and week presentation stays usable on a phone while existing generation, swap, recipe, preferences, and shopping-list workflows continue to work."
  artifacts:
    - path: frontend-new/app/meals/page.tsx
      provides: "Today/This week planner presentation, meal-log capture, adaptation feedback, and responsive layouts"
    - path: frontend-new/app/api/meal-log/route.ts
      provides: "Authenticated, validated meal-log persistence"
  key_links:
    - from: frontend-new/app/meals/page.tsx
      to: /api/meal-log
      via: "authenticated POST when a meal is marked eaten"
      pattern: "authFetch.*api/meal-log"
    - from: frontend-new/app/meals/page.tsx
      to: meal_logs
      via: "existing user-scoped Supabase history query drives completion and adaptation state"
      pattern: "from\\(\\\"meal_logs\\\"\\)"
---

<objective>
Deliver Ovona's first product-redesign slice: a low-friction Today-first meal experience, visible This week context, meal completion capture, and responsive refinement while retaining the current reliable planner.

Purpose: Help a user act on the next meal with less scanning and let Ovona quietly learn from completion.
Output: A redesigned meals page backed by the existing meal-log persistence contract.
</objective>

<context>
@AGENTS.md
@.planning/STATE.md
@frontend-new/app/meals/page.tsx
@frontend-new/app/api/meal-log/route.ts
@frontend-new/lib/meal-logs.ts

<interfaces>
From frontend-new/app/api/meal-log/route.ts:
```typescript
POST /api/meal-log
// body: { mealInstanceId: string; date: string; status: string; notes?: string | null }
// authenticated upsert unique on (user_id, meal_instance_id, date)
```

From frontend-new/lib/meal-logs.ts:
```typescript
export type MealLogStatus = "planned" | "eaten" | "skipped" | "swapped";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Today-first and This week meal presentation</name>
  <files>frontend-new/app/meals/page.tsx</files>
  <behavior>
    - A plan containing today's date selects that day by default and renders a prominent Today summary with its meals, macro context, and next-action cues.
    - A user can move from Today to any planned day through a compact This week date/day selector; the selected day's cards and recipe expansion remain available.
    - Existing generate, regenerate-week/day, swap, shopping-list, recipe, and preference controls retain their behaviors and access to the full weekly plan.
  </behavior>
  <action>Refactor the authenticated-plan render in `MealsPageContent` into a calm Today-first hierarchy. Derive a stable selected day from the existing `displayDays` and current date, add a compact This week navigation control that works with keyboard and touch, and render the selected day's meal cards in the primary flow before the secondary weekly context. Preserve all existing planner handlers and data calculations; do not replace generation, swapping, shopping, or preference persistence with mocked or local-only behavior. Keep date formatting timezone-safe by using the page's established date helpers and show a useful fallback when the stored workweek does not contain today.</action>
  <verify>
    <automated>npm run lint -- --file app/meals/page.tsx; npm run build</automated>
  </verify>
  <done>The meals page leads with Today's actionable meals, exposes every generated workday through This week navigation, and all pre-existing planner actions remain rendered and connected to their existing handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lightweight eaten capture and adaptation feedback</name>
  <files>frontend-new/app/meals/page.tsx, frontend-new/app/api/meal-log/route.ts</files>
  <behavior>
    - Marking a meal as eaten sends its instance ID, planned date, and `eaten` status through the authenticated meal-log route, then updates the UI without a page reload.
    - A failed save leaves the meal actionable and gives a retryable, clear error rather than falsely presenting it as complete.
    - Completion state and the existing meal-history data produce brief, calm feedback about what Ovona will adapt, without claiming nutritional outcomes that were not measured.
    - Invalid meal-log statuses are rejected by the API with a 400 response.
  </behavior>
  <action>Use the existing `meal_logs` table and `/api/meal-log` upsert contract—do not add a second tracking store. In the meals page, load/retain per-meal log state for the displayed week, add an accessible `I ate this` action on planned meal cards, protect it with a per-meal pending state, and optimistically reconcile only after a successful authenticated request. Update the existing history/adaptation panel and completion UI from actual saved statuses, using concise reassurance such as that future suggestions will reflect logged meals; do not fabricate calorie adherence or health progress. Harden the route by allowlisting the `MealLogStatus` values, validating required strings/date shape before Supabase, and preserving its user-scoped auth and upsert key.</action>
  <verify>
    <automated>npm run lint -- --file app/meals/page.tsx --file app/api/meal-log/route.ts; npm run build</automated>
  </verify>
  <done>Users can record eating a displayed meal once, see persisted completion/adaptation feedback, retry a failed save, and the API rejects malformed or unsupported log payloads while preserving authorized upsert behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Polish the responsive planner without reducing feature access</name>
  <files>frontend-new/app/meals/page.tsx</files>
  <action>Apply responsive MUI `sx` refinements to the redesigned page: stack Today content and meal controls cleanly at small breakpoints, use touch-friendly action sizes, prevent long meal/macro labels from overflowing, and retain readable hierarchy, focus states, and accessible labels. Keep the desktop weekly overview efficient and ensure the drawer, shopping accordion, macro cards, recipe details, swapping overlays, and regeneration controls remain reachable on narrow screens. Reuse the existing dark theme and spacing/color conventions rather than adding a second design system.</action>
  <verify>
    <automated>npm run lint -- --file app/meals/page.tsx --file app/api/meal-log/route.ts; npm run build</automated>
  </verify>
  <done>The complete meal-planning workflow is comfortable at mobile and desktop breakpoints, with no clipped primary actions or inaccessible existing features.</done>
</task>

</tasks>

<verification>
From `frontend-new`, run `npm run lint -- --file app/meals/page.tsx --file app/api/meal-log/route.ts` and `npm run build`. Then use the in-app browser against a running authenticated local session to verify the visual flow: navigate Today and This week, mark one meal eaten and refresh to confirm persistence, exercise swap, recipe, shopping, and preference controls, then repeat the primary flow at phone and desktop viewport widths. Confirm no primary action is clipped and no existing planner feature is hidden by the redesign.
</verification>

<success_criteria>
- Today is the default actionable surface and This week exposes the full generated workweek.
- Eaten status is persisted through the existing authenticated endpoint and visibly informs calm adaptation feedback.
- Generation, swaps, day/week regeneration, recipes, shopping, and preferences remain functional.
- The page lint passes and primary controls remain usable at phone and desktop widths.
</success_criteria>

<output>
After completion, create `.planning/quick/260811-o94-implement-ovona-first-product-redesign-s/260811-o94-SUMMARY.md`.
</output>
